name:
  en: Approval
  zh: 审批中心
description:
  en: Support various leave, overtime, and business trip requests.
  zh: 支持各类请假、加班、外出等申请。
author: DooTask
website: https://www.dootask.com/
tags:
  - 审批

# 知识库：随包发布的离线 chunk（ai-kb/<语言>/<类型>/<功能>/*.md），供 AI 检索。
knowledge_base: ./ai-kb

# 应用指令接口（doo app call / AI）：静态规范随版本目录发布，调用目标为 approve 服务容器。
# 规范文件实际放在版本目录（如 0.2.0/openapi.yaml），readSpecFile 会优先在版本目录解析。
openapi:
  file: ./openapi.yaml
  service: approve
  port: 3000
