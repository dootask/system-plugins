---
id: approve.design-template.howto
title: 设计审批模板（表单 + 流程）
type: howto
feature: approve
scope: admin
locale: zh
aliases:
  - 新建审批模板
  - 编辑模板
  - 设计表单
  - 设计审批流程
  - 配置审批人
  - 删除模板
  - 停用模板
related_tools: []
related_pages: [application]
prerequisites:
  - 应用市场已安装 approve 插件
  - 当前用户是管理员
negative:
  - 仅管理员能管理模板，普通用户看不到「模板管理」
  - 删除模板有二次确认；已发起的审批单不受删除影响
last_verified: v1.7.90
---

# 设计审批模板（表单 + 流程）

## 入口
- 「应用」→「审批」→「模板管理」（仅管理员可见）

## 模板列表能做什么
- 按名称搜索、按启用 / 停用状态筛选
- 新建模板、编辑模板
- 启用 / 停用模板（停用后用户无法再用它发起，已发起的不受影响）
- 删除模板（点删除后有二次确认）

## 新建 / 编辑模板
进入模板设计器，配置以下内容：
1. **基础信息**：模板名称、图标、所属分类（假勤 / 行政 / 财务 / 人事 / 其他）
2. **表单设计**：用表单设计器拖拽添加字段，设置标签、是否必填、选项、校验规则等。支持 14 种字段类型，详见 [[approve.form-field.concept]]
3. **流程设计**：用流程设计器搭审批流程

## 流程设计器
- 从发起节点开始，向下逐个添加节点：**审批节点**、**抄送节点**、**条件分支**
- 审批节点配置：节点名、审批人来源（指定成员 / 部门主管按层级 / 发起人本人）、多人时的审批方式（或签 / 会签 / 依次）
- 条件分支配置：按表单的数字 / 金额 / 单选字段设条件，把流程导向不同后续，必须留一条默认分支，详见 [[approve.route.concept]]
- 保存前会校验流程合法（至少要有审批节点、审批人不为空、分支要有默认分支等）

## 保存
设计完点保存即生效，用户在「发起申请」里立即能选到（前提是模板为启用状态）。

## 相关
- 模板是什么：[[approve.template.concept]]
- 表单字段：[[approve.form-field.concept]]
- 流程节点与审批方式：[[approve.node.concept]]
- 条件分支：[[approve.route.concept]]
